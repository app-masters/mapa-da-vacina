import PlaceQueueTemplate, { PlaceQueueProps } from './template';

/**
 * PlaceQueue
 */
const PlaceQueue: React.FC<PlaceQueueProps> = (props) => <PlaceQueueTemplate {...props} />;

export default PlaceQueue;
