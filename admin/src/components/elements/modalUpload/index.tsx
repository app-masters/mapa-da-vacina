import ModalUploadTemplate, { ModalUploadTemplateProps } from './template';

/**
 * ModalUpload
 */
const ModalUpload: React.FC<ModalUploadTemplateProps> = (props) => {
  return <ModalUploadTemplate {...props} />;
};

export default ModalUpload;
